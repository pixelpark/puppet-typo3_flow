# Managed by Puppet

TYPO3:
  Flow:
    persistence:
      backendOptions:
        driver: 'pdo_mysql'
        host: '<%= @db_host -%>'
        dbname: '<%= @db_name -%>'
        user: '<%= @db_user -%>'
        password: '<%= @db_pass -%>'
        charset: 'utf8'
